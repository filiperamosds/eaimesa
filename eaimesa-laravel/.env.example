APP_NAME=EaiMesa
APP_ENV=local
APP_KEY=
APP_DEBUG=true
# Origem do Next (CORS + links de claim). A API Laravel: php artisan serve --port=8000
APP_URL=http://localhost:3000

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=pt_BR

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=eaimesa
DB_USERNAME=eaimesa
DB_PASSWORD=eaimesa

SESSION_DRIVER=array
QUEUE_CONNECTION=sync
CACHE_STORE=file
FILESYSTEM_DISK=local

OWNER_JWT_SECRET=change-me-min-32-chars-random-owner
OWNER_JWT_TTL_HOURS=12
GUEST_SESSION_SECRET=change-me-min-32-chars-random-guest
GUEST_SESSION_TTL_HOURS=4
PIN_JOIN_MAX_FAILURES=5
PIN_JOIN_WINDOW_MINUTES=15
PLATFORM_JWT_SECRET=change-me-min-32-chars-random-platform
PLATFORM_JWT_TTL_HOURS=12
CLAIM_TTL_SECONDS=180
CHECKOUT_STUB_DELAY_MS=2000
